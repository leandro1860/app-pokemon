export type Types = {
    slot: string;
    type: {
        name: string;
        url: string;
    };
};

export type AbilityTypes = {
    ability: {
        name: string;
        url: string;
    };
    is_hidden: boolean;
    slot: boolean;
};
